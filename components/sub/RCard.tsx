"use client";

import React, { useId } from "react";
import { motion } from "motion/react";

export function Registrations() {
  const id = useId();

  return (
    <>
      
    </>
  );
}
export default Registrations;
// cards data remains the same
